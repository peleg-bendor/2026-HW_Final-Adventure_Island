// How much a category is allowed to say, ascending. Error is the floor rather than a value called
// Off: a category can be quietened down to only its real problems, never past them.
public enum LogLevel
{
    Error,
    Warning,
    Info,
    Verbose
}
