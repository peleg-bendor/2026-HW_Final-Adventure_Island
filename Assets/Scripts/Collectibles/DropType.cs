// What an enemy or an egg is configured to yield. None comes first so that anything left untouched
// in the Inspector drops nothing, which is one of the authored options rather than an oversight.
public enum DropType
{
    None,
    Heart,
    Leaf,
    Star,
    Axe,
    Boomerang,
    Fairy
}
