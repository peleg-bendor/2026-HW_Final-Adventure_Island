using UnityEngine;

// Identifies the player to anything that has to ask what touched it. A marker component rather
// than a tag, since most callers want the object itself and not just the answer.
public class Player : MonoBehaviour
{
}
